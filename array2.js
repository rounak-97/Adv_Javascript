// slice from the array

let a = [1, 2, 3, 4, 5, 6, 7, 8]

// slice(index of the starting slice element,position of the element)
    // let b=a.slice(1,5)
    // // let p=a.slice(2,7)
    // console.log(b)




// splice from the array..........................



// splice(index of the starting splice point ,number of element positions we want to splice)
    // let b=a.splice(3,2)
    // console.log(b)


// to replace the splice elements
    let r=a.splice(3,2,"ab","b")
    console.log(a)


// to add element at any place using splice()
    // let r = a.splice(4, 0, "ab")
    // console.log(a)


// primitive data types = null,undefined,number,string,BigInt,boolean,symbol
// non-primitive data types = object,array,function

    // typeof() = to know the data type of the variable


// bigint(long/large integer)
// ex- let b=2345n